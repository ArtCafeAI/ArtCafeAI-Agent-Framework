# Core dependencies
pyyaml>=6.0
aiohttp>=3.8.0
websockets>=10.4
tiktoken>=0.3.3
cryptography>=38.0.0

# Messaging providers (optional - install as needed)
nats-py>=2.5.0      # For NATS provider

# Optional LLM providers (install as needed)
# openai>=1.0.0       # For OpenAI provider
# boto3>=1.26.0       # For Bedrock provider
# anthropic>=0.20.0   # For Anthropic provider

# Development dependencies (install with pip install -e ".[dev]")
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.1.0
# mypy>=1.0.0
# flake8>=6.0.0
# isort>=5.12.0